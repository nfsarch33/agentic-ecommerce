# v1.8.0 Full-Stack QA Baseline

Generated: 2026-05-08

## Scope

- Backend: `agentic-ecommerce` at `680ff34`
- Frontend: `agentic-ecommerce-web` at `be7fa39` plus local v1.8 QA/security/SEO working-tree changes
- Plan source: `~/.cursor/plans/ec_stack_v2.0.0_plan_fadaaaa3.plan.md`

## Backend Evidence

- Coverage: `make coverage` passed with race detector; total statement coverage `80.4%`.
- Test count: `go test -json ./...` reported `612` passing tests across `36` packages and `0` failed tests.
- Contract tests: `go test -run 'Contract|OpenAPI' -count=1 -v ./cmd/mc-api` passed `14` top-level OpenAPI/contract tests covering products, orders, cart, sync, content, compliance, RAG/fact-check, media, webhooks, agent schedules, and recent events.
- Performance smoke: `make release-perf-smoke` passed.
  - `GET /api/v1/products`: p95 `1.265334ms`, target `<200ms`.
  - `POST /api/v1/auth/login`: p95 `440.208us`, target `<300ms`.
  - `POST /api/v1/products/{id}/generate-description`: p95 `1.042792ms`, target `<500ms`.
- Docker image: `docker build --build-arg TARGET=mc-api -t agentic-ecommerce:v180-qa .` passed; image size `8,898,146` bytes, about `8.5 MiB`.
- DB query audit: `make db-perf-audit` passed after `docker compose -f docker-compose.dev.yml up -d --wait postgres`, migrations, and seed.
  - Product catalog uses `idx_products_created_at`; execution `0.137ms`.
  - Slug lookup uses `idx_products_slug`; execution `0.025ms`.
  - Tenant catalog uses `idx_products_tenant_id` plus bounded in-memory sort; execution `0.328ms`.
  - Order detail uses `idx_orders_created_at`, `orders_pkey`, and `idx_order_items_order_id`; execution `0.196ms` on empty order seed.
  - Cart lookup uses `carts_pkey` and planned `idx_cart_items_session_id`; execution `0.038ms`.
  - RAG vector search uses `idx_rag_document_chunks_tenant_model`; execution `0.037ms` with no seeded chunks. Add a seeded vector fixture before treating IVFFlat behavior as proven.

## Frontend Evidence

- Unit/component coverage: `bun run test:coverage` passed.
- Test count: Vitest JSON reporter reported `365` total tests, `365` passed, `0` failed, across `206` suites.
- Type/lint/build: `bun run typecheck && bun run lint && bun run build` passed.
- Bundle budget: `reports/bundle/next-build-summary.json` reports max First Load JS `117 kB` on `/admin/products/[id]/content`, below the `200 kB` budget.
- Lighthouse: production standalone server plus `bun run qa:lighthouse` on `/products` passed with performance `100`, accessibility `100`, best practices `96`, SEO `100`.
- E2E: `bun run test:e2e:stable` is not yet green. Best clean run after switching the mock harness to plain `next dev` reported `16` passed, `1` skipped, `3` failed, and `1` flaky. Remaining failures are in admin agent dashboards and webhook form expectations, with workflow status flaky. This blocks calling the frontend v1.8 baseline green.

## Security Refresh

- Backend `gitleaks detect --source=. --no-git --redact --no-banner`: passed, no leaks.
- Backend `govulncheck ./...`: passed, no vulnerabilities.
- Frontend `bun run qa:security`: passed.
  - `gitleaks`: passed, no leaks.
  - `bun audit --audit-level=high`: passed, no vulnerabilities.
  - `trivy fs`: skipped because `trivy` is not installed locally.
  - `runx shell-leak-scan --repo agentic-ecommerce-web`: passed, no findings.
  - `sentrux gate .`: passed, quality `6614 -> 6631`, no degradation.

## Repeatable QA Commands

- Backend already exposes `make qa-v180`, `make contract-test`, `make load-test`, `make db-perf-audit`, `make security-refresh`, `make shell-leak`, and `make sentrux-gate`.
- Frontend already exposes `bun run qa:v180`, `bun run qa:bundle`, `bun run qa:lighthouse`, `bun run qa:security`, and `bun run test:e2e:stable`.

## Follow-ups for v1.9 Tenant-Aware Admin

- Add seeded RAG chunks to the DB performance audit so vector index behavior is measured with realistic row counts.
- Add tenant-scoped E2E fixtures for admin agents, webhooks, workflows, media, and compliance dashboards before tenant selector work lands.
- Extend backend contract tests with tenant claim/header permutations and negative isolation cases.
- Fix the remaining frontend Playwright blockers before using v1.8 as the v1.9 regression baseline.
- Install Trivy locally or add a containerized Trivy fallback to make high/critical filesystem scans mandatory instead of skipped.
